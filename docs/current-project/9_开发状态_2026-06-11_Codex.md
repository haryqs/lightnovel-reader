# 阅读器开发状态与进度

> 日期: 2026-06-11  
> 接手者: Codex  
> 仓库: `E:\workspace\game-cooperative-plan`  
> 阅读器代码目录: `reader/`

## 一、当前仓库状态

- 当前本地分支: `main`
- 当前本地 HEAD: `1984781 docs: 交接文档(转交 Codex)+ AGENTS.md 同步当前架构与纪律`
- 远端 `origin/main`: `67b77f1 同步当前项目状态`
- 分叉状态: 本地 `main` 比 `origin/main` 多 37 个提交,同时落后 1 个宿舍端提交。
- 本轮推送策略: 不强推、不覆盖 `origin/main`;状态文档推送到 `codex/reader-status-2026-06-11` 分支。
- 本轮完整代码提交: 本地分支 `codex/reader-v03-library-status` 上的 `e061c8d reader: 接线 v0.3 本地书库并记录开发状态`。该分支包含本地比远端多出的历史,直接推送会上传大量对象,需先整理远端/main 分叉后再推完整代码线。

## 二、产品与架构状态

当前路线已经收敛为:

```text
reading-core(Rust 跨端核心)
  + reader-engine(TypeScript 阅读引擎)
  + Tauri v2 桌面薄壳
  + 后续 Android/iOS/鸿蒙薄壳
```

强制纪律仍然有效:

1. 前端不得直接 import `@tauri-apps/*`,只能通过 `reader/src/platform/`。
2. 业务逻辑下沉到 `reader/crates/reading-core`。
3. 改桥接协议必须同步改 TS 类型、Rust serde 结构和协议文档。

## 三、已完成能力

### v0.2 阅读内核

- EPUB 解析与 HTML 清洗。
- 虚拟页分页。
- 单页/双页阅读模式。
- 三套主题与字号/版式设置。
- 目录跳转。
- 高亮/批注/书签持久化。
- Markdown 标注导出。
- 阅读进度持久化与开书恢复。
- Calibre 书库只读浏览与按路径开书。

### 架构改造

- `reading-core` 已抽成独立 Rust crate。
- Tauri command 只做胶水。
- 前端 platform 适配层已落地。
- `scripts/check-arch.mjs` 已接入 `npm run build`,强制检查 Tauri API 边界。
- 插件 SDK 契约已入库,运行时排期 v0.7。

### v0.3 书库底座

- `reading-core::library` 已落地:
  - `books` 表。
  - SHA-256 对象仓库去重。
  - `objects/<id>.epub` 入库保存。
  - FTS5 trigram 搜索。
  - 短查询 LIKE 兜底。
  - `last_read_at` 最近阅读排序。

## 四、本轮 Codex 已完成

本轮从 `当前项目_阅读器/8_交接_转交Codex.md` 第五节开始执行。

### 1. 冒烟测试

- `npm run tauri dev` 成功启动到 `reader` 进程。
- 受当前自动化环境限制,未能可靠操作外部 GUI 窗口。
- 仍需人工实机验证:
  - 开书。
  - 翻页。
  - 关闭重开后恢复阅读位置。
  - 高亮保存后重开仍可见。

### 2. v0.3 书库接线闭环

新增桥接协议:

- `library.import`
- `library.list`
- `library.search`
- `library.open`
- `library.touchLastRead`

Tauri 侧新增:

- 独立 `library.sqlite`。
- `library/objects/` 对象仓库目录。
- `library_import`
- `library_list`
- `library_search`
- `library_open`
- `library_touch_last_read`

前端新增/调整:

- 书库视图从“Calibre 书库”改为“本地书库”。
- Calibre 降级为导入来源。
- 支持从 Calibre 来源批量导入 EPUB。
- 自有书库支持列表、搜索、按 id 打开。
- 打开自有书库书籍后更新最近阅读时间。

同步文档:

- `workspace/reader-resource-library-plan/8_桥接协议_v0.1.md`

## 五、验证结果

已通过:

```powershell
npm run build
cargo test --workspace
git diff --check
```

结果:

- `npm run build` 通过。
- `check-arch` 通过。
- `cargo test --workspace` 通过,15/15。
- `git diff --check` 通过,仅有 Windows LF/CRLF 提示。

## 六、当前未解决项

### 必须人工验证

- Tauri 桌面窗口完整冒烟:
  - 打开本地 EPUB。
  - 连续翻页。
  - 拖动进度。
  - 关闭重开恢复进度。
  - 高亮/批注保存并重开验证。
  - 从“本地书库”导入 Calibre 来源后开书。

### v0.3 后续补齐

1. 书库导入交互优化:
   - 导入进度不应长时间占满书架区域。
   - 大书库批量导入需要更细的失败报告。
2. 单本 EPUB 直接导入入口:
   - 当前主要路径是 Calibre 批量导入。
   - 后续可以让“打开 EPUB”旁边提供“导入到书库”。
3. 书库 v1.1:
   - 封面提取: OPF manifest cover-image -> covers/。
   - `books.cover_path` 回填。
   - series 元数据: `calibre:series` / series index。
4. v0.4 标注增强:
   - 跨元素选区高亮当前仍有降级。
5. v0.5:
   - 在线元数据。
   - 桥接协议冻结 1.0。

## 七、注意事项

- 不要直接把本地 `main` 强推到 `origin/main`。
- 当前本地 `main` 与远端 `origin/main` 有分叉,合并前必须单独审查远端 `67b77f1`。
- 不要提交以下未跟踪本地配置:
  - `.claude/settings.local.json`
  - `workspace/reader-resource-library-plan/.claude/`
- 如果继续做阅读性能优化,不要再只微调 CSS/DOM;下一层杠杆是结构化章节块、分页切片缓存或 off-main-thread pagination。
