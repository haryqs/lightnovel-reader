# 当前状态与里程碑

## 已完成

| 版本 | 内容 | 日期 |
|------|------|------|
| v0.1.0 | Tauri v2 骨架创建 | 2026-06-05 |
| v0.1.0 | npm 依赖 + epubjs 基础渲染 | 2026-06-05 |
| v0.1.0 | 三套护眼主题（日/护眼/夜） | 2026-06-05 |
| v0.1.0 | 分页阅读 + TOC 目录 + 进度条 | 2026-06-05 |
| v0.1.0 | 阅读进度持久化（localStorage） | 2026-06-05 |
| — | Rust 1.96.0 (stable-msvc) 安装 | 2026-06 |
| — | VS 2022 BuildTools (C++ workload) 安装 | 2026-06 |
| — | `tauri dev` 编译成功（360 crates, 4m31s） | 2026-06-05 |

## 路线图

```
v0.1 ────→ v0.2 ────→ v0.3 ────→ v0.4 ────→ v1.0
骨架跑通    Rust解析    标注        Calibre     稳定发布
 ✓         ↓           ↓           ↓
```

### v0.2 —— Rust EPUB 解析（替换 epubjs）

**目标**：用 Rust 端解析 EPUB，前端只做渲染。

**任务**：
- [ ] Rust 端：ZIP 解压 + OPF 解析（`zip` + `quick-xml`）
- [ ] Rust 端：返回 `{metadata, toc, spine}` 给前端
- [ ] Rust 端：按章提取 XHTML，暴露为 Tauri command
- [ ] 前端：移除 `epubjs` 依赖
- [ ] 前端：CSS Columns 基础分页器
- [ ] 验证：打开一本 epub，目录正确，分页正常

**估时**：3-5 天

---

### v0.3 —— HTML 清洗 + 旧 EPUB 优化

**目标**：Rust 端清洗 HTML，让格式差的旧 EPUB 也能舒适阅读。

**任务**：
- [ ] 剥离原始 CSS
- [ ] 注入统一排版（行高 1.8、字体缩放）
- [ ] 编码检测 + 转换（GB2312 → UTF-8）
- [ ] 图片路径修复（映射为 `epub://` 自定义协议）
- [ ] 去除 `<font>`、`<br>` 分段等垃圾标签

**估时**：3-5 天

---

### v0.4 —— 标注系统

**目标**：高亮划线 + 批注 + localStorage/SQLite 持久化。

**任务**：
- [ ] 文本选择 → 高亮（SVG overlay）
- [ ] 批注输入面板
- [ ] CFI 定位 + 持久化
- [ ] 标注列表侧栏

**估时**：3-5 天

---

### v0.5 —— 标注导出

**目标**：标注导出为 Markdown，Obsidian 可直接使用。

**任务**：
- [ ] Markdown 格式生成器
- [ ] 导出文件写入本地磁盘（Tauri fs plugin）
- [ ] 可选：导出为 Markdown + 高亮色标记

**估时**：1-2 天

---

### v0.6 —— Calibre 书库浏览

**目标**：浏览本地 Calibre 书库，点击即读。

**任务**：
- [ ] Rust 端：`rusqlite` 读取 `metadata.db`
- [ ] 前端：书架视图（封面 + 书名 + 作者）
- [ ] 点击书籍 → 自动打开对应的 epub 文件
- [ ] 搜索/筛选

**估时**：3-5 天

---

### v1.0 —— 稳定发布

**目标**：以上功能稳定，代码清理，打包为可分发安装包。

**估时**：1-2 周（含测试和修复）

---

## 长期（v2.0+）

- PDF 支持
- 远程 Calibre（OPDS）
- 插件系统
- 移动端（Tauri mobile for iOS/Android）
