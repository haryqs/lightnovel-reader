{
  "name": "reader",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check:arch": "node scripts/check-arch.mjs",
    "check:memory": "node scripts/check-dev-memory.mjs",
    "check:project": "node scripts/check-arch.mjs && node scripts/check-dev-memory.mjs",
    "dev:note": "node scripts/dev-note.mjs",
    "build": "node scripts/check-arch.mjs && tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
